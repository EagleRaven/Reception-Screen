// Refresh Page at set Time

function refresh(){
    location.reload();
}

// Currently set to refresah when weather does one rotation. Change as neccessary.
setInterval(refresh, 320000); // 1000 = 1 second, 600000 = 10 mins